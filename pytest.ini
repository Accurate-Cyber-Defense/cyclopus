[pytest]
addopts = -v --tb=short --strict-markers --disable-warnings
testpaths = tests
python_files = test_*.py commands-test.py
python_classes = Test*
python_functions = test_*
markers =
    unit: Unit tests
    integration: Integration tests
    slow: Slow tests
    network: Network tests
    docker: Docker tests
    security: Security tests